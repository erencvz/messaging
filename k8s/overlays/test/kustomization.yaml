apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: messaging-test

resources:
  - ../../base

configMapGenerator:
  - name: messaging-api-env
    literals:
      - ENVIRONMENT=test

images:
  - name: ghcr.io/YOUR_ORG/messaging-api
    newTag: latest
